Upgrade AGP version 1 by 1 - implement the new features in each version
Update all libraries 1 by 1 - implement the api changes in each version
Add environment variables for the API KEYS etc.
Upgrade targetSDK, minSDK and compileSDK
Remove firebase (should we?)
Update the package name

Update the navigation library code to follow the latest best practices (updated from 2.3.5)
Use the latest gradle features since Gradle 5.0
Remove buildConfig = true from the modules where we shouldn't have build configs

Replace databinding with viewBinding
Migrate UI to compose
Migrate DI to latest Koin then to Dagger
Migrate LiveData to Flow
Write Unit tests
Write unit UI tests
Write Instrumented tests
